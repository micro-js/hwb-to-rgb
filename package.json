{
  "name": "@f/hwb-to-rgb",
  "description": "Convert HWB colors to RGB",
  "repository": "git://github.com/micro-js/hwb-to-rgb.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/hsl-to-rgb": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
